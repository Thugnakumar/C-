#include <iostream>
#include <cstring>
#include <vector>
#include "student.h"
#include "node.h"

using namespace std;

int main(){
  Node* student1 = new Node(new Student());
  Node* student2 = new Node(new Student());
}
